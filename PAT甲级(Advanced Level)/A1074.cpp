#include <cstdio>
struct Node {
	int address;
	int data;
	int next;
}node[100010];

bool cmp
int main() {
	int startAdd, n, k;
	for(int i = 0; i < n; i++) {
		scanf("%d %d %d", &node[i].address, &node[i].data, &node[i].next);
	}
	
	return 0;
}
